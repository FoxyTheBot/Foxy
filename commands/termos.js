const Discord = require('discord.js')
const config = require('../config.json');

module.exports.run = async (client, message, args) => {
    message.channel.send("Todas as dicas dadas nesse comando são bem pesquisadas **ENTÃO NÃO VENHA RECLAMAR COMIGO (WINGAMER)!**")
  };